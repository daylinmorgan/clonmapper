name: GitHub Pages

on:
  push:
    branches:
      - main  # Set a branch name to trigger deployment

jobs:
  deploy:
    runs-on: ubuntu-latest
    container:
      image: daylinmorgan/pandoc
    permissions:
      contents: write
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # - name: work around permission issue
      #   run: git config --global --add safe.directory /__w/clonmapper-protocol/clonmapper-protocol

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Setup Mkdocs
        run: nix develop --command make bootstrap

      - name: Build Docs
        run: nix develop --command make docs.build

      # - name: Deploy
      #   uses: peaceiris/actions-gh-pages@v3
      #   # If you're changing the branch from main,
      #   # also change the `main` in `refs/heads/main`
      #   # below accordingly.
      #   if: ${{ github.ref == 'refs/heads/main' }}
      #   with:
      #     personal_token: ${{ secrets.PERSONAL_TOKEN }}
      #     external_repository: brocklab/clonmapper
      #     publish_dir: ./site/public
