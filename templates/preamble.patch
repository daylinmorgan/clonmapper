--- templates/default.latex	2024-03-13 15:27:56.768559397 -0500
+++ templates/default.tex	2024-03-13 15:20:31.625230616 -0500
@@ -494,6 +494,34 @@
 $endif$
 $endif$
 
+
+% custom preamble
+
+\usepackage{adjustbox}
+\renewcommand{\familydefault}{\sfdefault}
+\usepackage[multiple]{footmisc}
+
+% need for \input{table.tex} to work
+\usepackage{calc} % for calculating minipage widths
+
+\usepackage{datetime2}
+$if(rev)$
+\AddToHook{shipout/foreground}{%
+  \put(1\baselineskip,-\paperheight+1\baselineskip)
+    % {\footnotesize Rev. $rev$ | Updated: \DTMtoday.}
+    {\footnotesize Rev. $rev$ }
+}
+$endif$
+
+\usepackage{siunitx}
+\DeclareSIUnit\molar{M}
+\DeclareSIUnit\mM{\milli\molar}
+\DeclareSIUnit\uM{\micro\molar}
+\sisetup{detect-weight=true, detect-family=true, per-mode=symbol}
+
+
+% end custom preamble
+
 \begin{document}
 $if(has-frontmatter)$
 \frontmatter
